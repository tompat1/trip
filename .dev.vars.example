# Copy to .dev.vars for local `wrangler dev`.
# Do not commit .dev.vars.

TRIP_ADMIN_EMAIL=admin@example.com
TRIP_ADMIN_PASSWORD=change-me

# Optional break-glass bearer token for local API testing:
# TRIP_ADMIN_TOKEN=change-me-too
