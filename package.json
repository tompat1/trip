{
  "name": "trip-travel-planner-deluxe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "start": "vite --host 127.0.0.1",
    "test": "node --test",
    "build": "vite build",
    "deploy": "vite build && npx wrangler deploy",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "vite": "latest"
  }
}
